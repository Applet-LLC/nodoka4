# Build output directories
Release/
Win32/
x64/
Debug/

# Visual Studio user-specific files
*.vcxproj.user
*.vcproj.*.user
.vs/

# Compiled outputs (except specifically tracked binaries)
*.obj
*.pdb
*.exp
*.lib
*.aps
*.tlog/
*.log
*.lastbuildstate
*.FileListAbsolute.txt
*.Build.CppClean.log
*.dll.recipe
vcpkg.applocal.log

# Backup and temporary files
*.bak
*.old
*.org
*.20260610
*.20260610A
*.20260610B
*.fix20260609

# WDK/DVL analysis artifacts
*.sarif
*.DVL.XML
*.hlkx
vc140.pdb
vc143.pdb
RCa*

# SVN metadata
.svn/

# AI tool artifacts
.cursor/
.specstory/

# Installer outputs (build artifacts in distrib)
distrib/*.exe
distrib/*.log
distrib/*.DDF
distrib/files/
distrib/temp/
distrib/opened/
distrib/Sample/

# Specific temp/intermediate files in distrib
distrib/__nodoka__.sed

# Vendored build-tool binaries (local build dependency, not source)
tools/SigRemover.exe
tools/iexpress.exe
tools/makecab.exe
tools/wextract.exe

# Driver build outputs (except tracked sys in d.sign)
error/
old/

# MSBuild incremental-build tracking (VC++ "Sample" up-to-date-check
# folders, SDK-style auto-generated assembly files)
Sample/
*.recipe
*_generated.flag
*.AssemblyReference.cache
*AssemblyAttributes.cpp

# L3 challenge-response private key (docs/driver-access-control-plan.md).
# Real key material must never be pushed. Only authkey_secret.example.h
# (dummy key, compiles but won't authenticate) is tracked.
nodoka/authkey_secret.h

# Internal planning/investigation notes. Kept locally, intentionally not
# published (removed from git history 2026-08-18).
/docs/
/検討や調査内容/
